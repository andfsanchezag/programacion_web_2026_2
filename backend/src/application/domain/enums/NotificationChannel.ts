/**
 * NotificationChannel - Technical enumeration representing the communication
 * channel used by the system to deliver notifications.
 */
export enum NotificationChannel {
  EMAIL = 'EMAIL',
  SMS = 'SMS',
  PUSH_NOTIFICATION = 'PUSH_NOTIFICATION',
}