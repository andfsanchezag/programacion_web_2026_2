# Loan Services

## Introduction

This document defines the services belonging to the **Loan** subdomain of the Banking Information Management System.

The services in this subdomain are responsible for managing the lifecycle of loans requested by customers, from the initial application through evaluation, approval or rejection, disbursement, cancellation, and closure when applicable.

Loans are represented by the `Loan` Domain Model and inherit from `BankingProduct`.

```text
BankingProduct
      │
      └── Loan
```

A loan is a banking product associated with a `Customer`.

Its lifecycle generates business operations that must be traceable through the `Operation` and `AuditLog` Domain Models.

The services described in this document define the conceptual behavior of the Loan subdomain.

Implementation details such as REST contracts, persistence mappings, JPA entities, SQL queries, MongoDB documents, HTTP clients, and infrastructure-specific mechanisms must be documented separately.

---

# Domain Model Context

A `Loan` represents a credit product requested by a customer.

Conceptually:

```text
BankingProduct
      │
      └── Loan
             │
             ├── applicant : Customer
             ├── loanType : LoanType
             ├── requestedAmount : BigDecimal
             ├── approvedAmount : BigDecimal
             ├── interestRate : BigDecimal
             ├── termInMonths : Integer
             ├── loanStatus : LoanStatus
             ├── approvalDate : LocalDate
             ├── disbursementDate : LocalDate
             └── destinationAccount : BankAccount
```

The applicant must be represented using the `Customer` Domain Model:

```text
Loan.applicant : Customer
```

The destination account must be represented using the `BankAccount` Domain Model:

```text
Loan.destinationAccount : BankAccount
```

The Domain Model must not replace these relationships with primitive identifiers such as:

```text
String customerId
String accountId
```

Persistence adapters are responsible for translating Domain relationships into their infrastructure representation.

---

# Value Objects Used by Loan

The Loan subdomain relies on the following Value Objects defined in the Domain Value Objects specification.

```text
Loan
 │
 ├── loanType       : LoanType
 ├── loanStatus     : LoanStatus
 ├── applicant      : Customer
 │      ├── status  : CustomerStatus
 │      └── role    : SystemRole
 │
 └── destinationAccount : BankAccount
        ├── accountType   : AccountType
        ├── accountStatus : AccountStatus
        └── currency      : Currency
```

The acting `User`, when applicable, also exposes:

```text
User
 ├── status : UserStatus
 └── role   : SystemRole
```

Business operations generated by Loan services use:

```text
Operation.operationType : OperationType
```

Relevant operation types include:

```text
LOAN_APPLICATION
LOAN_APPROVAL
LOAN_REJECTION
LOAN_DISBURSEMENT
LOAN_PAYMENT
LOAN_OVERDUE
LOAN_CANCELLATION
```

---

# Value Object Validation Principles

Value Objects are part of the Domain Model and must not be treated as arbitrary strings.

For example:

```text
LoanType
LoanStatus
CustomerStatus
UserStatus
AccountStatus
AccountType
Currency
OperationType
SystemRole
```

must represent controlled business values.

The service must not validate them through arbitrary string comparisons such as:

```java
loan.getLoanStatus().equals("APPROVED");
```

Instead, the Domain Model must work with the corresponding Value Objects.

Conceptually:

```java
loan.getLoanStatus().equals(LoanStatus.APPROVED);
```

or, preferably, through expressive Domain behavior:

```java
loan.isApproved();
loan.canBeDisbursed();
loan.canBeCancelled();
```

The exact implementation mechanism is an implementation decision, but the business concept must remain inside the Domain.

---

# DomainCatalog Rules

All catalog Value Objects inherit conceptually from:

```text
DomainCatalog
```

with:

```text
code
name
description
```

Catalog Value Objects must be:

* Immutable.
* Comparable by value.
* Restricted to valid business values.
* Identified by a unique business code.
* Independent from persistence technology.
* Independent from REST representation.

A Loan service must never create arbitrary catalog values such as:

```text
new LoanStatus("ANY_VALUE")
```

unless the Domain provides a controlled factory capable of enforcing catalog invariants.

---

# Monetary Representation

All monetary attributes in the Loan Domain Model remain represented as:

```text
BigDecimal
```

No `Money` Value Object is introduced by this specification.

The following attributes remain `BigDecimal`:

```text
requestedAmount : BigDecimal
approvedAmount  : BigDecimal
```

`interestRate` is also represented as:

```text
BigDecimal
```

because it is a business numeric value rather than a monetary amount.

---

# Monetary Validation Rules

The Domain must enforce the following invariants.

## Requested Amount

```text
requestedAmount != null
requestedAmount > 0
```

The amount must represent a positive financial value.

Zero or negative requested amounts are invalid.

---

## Approved Amount

Before approval:

```text
approvedAmount == null
```

or, if the business process allows a preliminary value, it must still satisfy the Domain constraints.

When a loan is approved:

```text
approvedAmount != null
approvedAmount > 0
```

Unless the business rules explicitly permit otherwise:

```text
approvedAmount <= requestedAmount
```

The approval service must not accept an independent `BigDecimal approvedAmount` parameter.

The amount must belong to the `Loan` Domain Model.

---

## BigDecimal Comparison

Because Java `BigDecimal.equals()` considers scale, Domain numerical comparisons must not rely on it when comparing monetary magnitude.

Conceptually:

```java
amount.compareTo(BigDecimal.ZERO) > 0
```

rather than:

```java
amount.equals(BigDecimal.ZERO)
```

---

## Monetary Precision

The Domain must define a consistent precision and scale policy for:

```text
requestedAmount
approvedAmount
interestRate
```

This policy must be applied consistently during:

* Validation.
* Calculation.
* Persistence mapping.
* Disbursement.
* Payment processing.

Floating-point types such as:

```text
double
float
```

must never be used for monetary values.

---

# Interest Rate Validation

`interestRate` remains:

```text
BigDecimal
```

The Domain must validate:

```text
interestRate != null
interestRate >= 0
```

The maximum permitted rate, precision, scale, and LoanType-specific rate rules must be defined by the corresponding business policies.

If interest-rate rules become sufficiently complex, a future `InterestRate` Value Object may be introduced.

This specification does not require that Value Object.

---

# Loan Term Validation

`termInMonths` remains:

```text
Integer
```

The Domain must validate:

```text
termInMonths != null
termInMonths > 0
```

Additional restrictions may depend on:

```text
LoanType
```

For example:

```text
LoanType
    │
    ├── PERSONAL
    ├── MORTGAGE
    ├── VEHICLE
    └── BUSINESS
```

Each LoanType may define:

* Minimum term.
* Maximum term.
* Minimum amount.
* Maximum amount.
* Interest-rate constraints.
* Customer-type restrictions.
* Additional eligibility requirements.

These rules belong to the Domain.

---

# Currency Validation

A Loan involving monetary values must have a well-defined currency.

If `BankingProduct` already provides:

```text
currency : Currency
```

the Loan inherits that value.

If `BankingProduct` does not contain currency, the Loan model must be extended to explicitly represent its currency.

A loan with monetary amounts but without an identifiable currency is incomplete for a multi-currency banking system.

The Domain must validate:

```text
Loan.currency != null
```

when currency is part of the applicable model.

---

# Currency Compatibility

During disbursement:

```text
Loan.currency == destinationAccount.currency
```

must hold unless the business explicitly supports foreign-exchange conversion.

If currencies differ and no FX business capability exists:

```text
CurrencyMismatchException
```

must be raised.

The Loan service must not silently convert currencies.

If currency conversion is introduced in the future, it must be represented through an appropriate Domain Policy and Output Port.

---

# CustomerStatus Validation

The applicant is represented by:

```text
Loan.applicant : Customer
```

The Customer's status is:

```text
Customer.status : CustomerStatus
```

For normal Loan processing, the applicant must satisfy:

```text
CustomerStatus.ACTIVE
```

The following states must not automatically be treated as equivalent:

```text
ACTIVE
INACTIVE
BLOCKED
```

Therefore:

```text
INACTIVE != BLOCKED
```

and the business rules may define different consequences for each.

Unless explicitly specified otherwise:

```text
INACTIVE → cannot initiate new loan applications
BLOCKED  → cannot initiate new loan applications
ACTIVE   → may proceed subject to eligibility rules
```

---

# UserStatus Validation

When a Loan operation is performed by a User, the service must validate:

```text
User.status
```

The normal requirement is:

```text
UserStatus.ACTIVE
```

A user with:

```text
INACTIVE
BLOCKED
```

must not execute protected Loan operations.

User status is independent from Customer status.

For example:

```text
Customer.status = ACTIVE
User.status     = BLOCKED
```

does not authorize the User to execute an operation.

Likewise:

```text
Customer.status = BLOCKED
User.status     = ACTIVE
```

does not make the Customer eligible.

---

# SystemRole Validation

Roles are represented by:

```text
SystemRole
```

Relevant roles include:

```text
NATURAL_CUSTOMER
BUSINESS_CUSTOMER
TELLER_EMPLOYEE
COMMERCIAL_EMPLOYEE
BUSINESS_OPERATOR
BUSINESS_SUPERVISOR
INTERNAL_ANALYST
```

Authorization must validate both:

```text
User.status
User.role
```

A role alone is insufficient.

For example:

```text
User.role = INTERNAL_ANALYST
User.status = BLOCKED
```

must not authorize loan approval.

---

# Important SystemRole Modeling Rule

`NATURAL_CUSTOMER` and `BUSINESS_CUSTOMER` represent customer classifications more than conventional authorization roles.

If `SystemRole` is also defined as the authorization role of a `Person`, the Domain must establish how these values relate to:

```text
NaturalCustomer
BusinessCustomer
```

The system must not use `SystemRole` as a substitute for determining the actual Customer subtype.

For Loan eligibility, the authoritative object is:

```text
Customer
```

and its concrete specialization:

```text
NaturalCustomer
BusinessCustomer
```

When a rule depends on the type of customer, the service must use the Customer Domain Model rather than inferring customer type exclusively from a role string.

---

# LoanType Validation

Valid Loan Types are:

```text
PERSONAL
MORTGAGE
VEHICLE
BUSINESS
```

The service must validate that:

```text
loan.loanType != null
```

and that it represents a valid controlled `LoanType`.

The service must not accept arbitrary strings.

---

# LoanType-Specific Eligibility

The following rules must be represented as Domain policies or specifications when applicable.

```text
LoanType
    │
    ├── PERSONAL
    ├── MORTGAGE
    ├── VEHICLE
    └── BUSINESS
```

Potential business constraints include:

```text
Customer type
Amount range
Term range
Interest rate range
Destination account requirements
Collateral requirements
Income requirements
Existing debt constraints
```

The current specification does not define exact numerical thresholds.

Therefore, services must not invent such thresholds.

They must delegate them to explicit Domain Policies/Specifications once the business rules are defined.

---

# AccountType Validation

The destination account exposes:

```text
BankAccount.accountType : AccountType
```

Valid values include:

```text
SAVINGS
CHECKING
BUSINESS
```

The Loan service must validate the account type when LoanType-specific disbursement rules require it.

For example:

```text
BUSINESS Loan
    ↓
may require
    ↓
BUSINESS Account
```

Such restrictions must be explicit Domain rules and must not be inferred from the database.

---

# AccountStatus Validation

The destination account exposes:

```text
BankAccount.accountStatus : AccountStatus
```

Valid states are:

```text
ACTIVE
BLOCKED
CLOSED
```

A Loan disbursement requires:

```text
AccountStatus.ACTIVE
```

The following states must reject disbursement:

```text
BLOCKED
CLOSED
```

The Loan service must not credit an account that cannot receive operations.

---

# Destination Account Ownership

The Loan service must validate the relationship between:

```text
Loan.applicant
```

and:

```text
Loan.destinationAccount
```

The service must not assume that the relationship is valid merely because both Domain Models are present.

Where the business rule requires the destination account to belong to the applicant, the service must validate:

```text
Loan.applicant
        │
        ▼
BankAccount.owner
```

The authoritative `BankAccount` may be retrieved through the corresponding Output Port when the supplied Domain Model is incomplete or stale.

---

# Loan Status Value Object

The Loan uses:

```text
LoanStatus
```

The supported states are:

```text
UNDER_REVIEW
APPROVED
REJECTED
DISBURSED
OVERDUE
CANCELLED
```

The Domain must be the authority for valid transitions.

---

# Loan Lifecycle

The lifecycle is:

```text
                 ┌──────────────► REJECTED
                 │
UNDER_REVIEW ────┼──────────────► CANCELLED
                 │
                 ▼
              APPROVED
                 │
                 ├──────────────► CANCELLED
                 │
                 ▼
             DISBURSED
                 │
                 ▼
              OVERDUE
                 │
                 ▼
             CANCELLED
```

A separate `CLOSED` state is not included in the current LoanStatus catalog.

Therefore:

```text
CloseLoanUseCase
```

must not be used unless `CLOSED` is explicitly added to `LoanStatus` and its lifecycle is formally defined.

The current service is therefore:

```text
Cancel Loan
```

with:

```text
CancelLoanUseCase
```

---

# State Transition Rules

The Domain must enforce:

```text
UNDER_REVIEW → APPROVED
UNDER_REVIEW → REJECTED
UNDER_REVIEW → CANCELLED

APPROVED → DISBURSED
APPROVED → CANCELLED

DISBURSED → OVERDUE

OVERDUE → CANCELLED
```

Invalid transitions must raise:

```text
InvalidLoanStatusTransitionException
```

The persistence layer must never determine whether a transition is valid.

---

# Lifecycle Ownership

The service must never allow an external caller to arbitrarily assign:

```text
loanStatus
approvalDate
disbursementDate
```

For example, a request must not be trusted merely because it contains:

```text
loanStatus = APPROVED
```

The service must establish the correct initial state through Domain behavior.

Likewise:

```text
approvalDate
```

must be established when the Domain performs the approval transition.

And:

```text
disbursementDate
```

must be established when the Domain performs the disbursement transition.

---

# Standard Loan Service Pattern

Every state-changing Loan service must follow the following conceptual pattern:

```text
1. Receive Domain Models
        │
        ▼
2. Retrieve authoritative state when necessary
        │
        ▼
3. Validate User
        │
        ▼
4. Validate Customer
        │
        ▼
5. Validate User-Customer relationship
        │
        ▼
6. Validate Loan
        │
        ▼
7. Validate Value Objects
        │
        ▼
8. Validate Loan status
        │
        ▼
9. Validate operation-specific business rules
        │
        ▼
10. Execute Domain behavior
        │
        ▼
11. Persist Domain Model
        │
        ▼
12. Create Operation
        │
        ▼
13. Create AuditLog
```

Not every service requires every step.

Only relevant validations must be executed.

The service must not introduce artificial validation use cases such as:

```text
ValidateUserService
ValidateOwnershipService
ValidateLoanStatusService
ValidateAccountService
```

when those validations are part of a larger cohesive business operation.

---

# Authoritative State Rule

For state-changing operations, the Loan supplied to the service represents the business context but must not automatically be considered the authoritative persisted state.

When required, the service retrieves the authoritative Loan through:

```text
LoanRepository
```

Conceptually:

```text
Incoming Loan
     │
     ▼
LoanRepository
     │
     ▼
Authoritative Loan
     │
     ▼
Business Validation
     │
     ▼
Domain Behavior
```

This prevents stale Domain Models from causing invalid state transitions.

---

# 1. Request Loan

## Description

Creates a new loan application for a Customer.

The loan enters:

```text
UNDER_REVIEW
```

The caller must not arbitrarily determine the initial lifecycle state.

---

## Input

```java
Loan request(Loan loan);
```

The Domain Model must contain:

```text
Applicant
LoanType
RequestedAmount
Term
DestinationAccount when applicable
Currency when applicable
```

---

## User Validation

If the request is initiated by an authenticated User, the service must validate:

```text
User.status == ACTIVE
```

and the appropriate authorization rule.

If the request is customer-initiated, the service must validate:

```text
User
   │
   ▼
Customer
   │
   ▼
Loan.applicant
```

The requesting Customer must correspond to the applicant.

---

## Customer Validation

Validate:

```text
loan.applicant != null
loan.applicant.status != null
loan.applicant.status == CustomerStatus.ACTIVE
```

Additional Customer information must be obtained through:

```text
CustomerRepository
```

only when it is not already available in the Domain Model.

---

## LoanType Validation

Validate:

```text
loan.loanType != null
```

and ensure that the value belongs to the supported `LoanType` catalog.

---

## Requested Amount Validation

Validate:

```text
requestedAmount != null
requestedAmount > 0
```

LoanType-specific amount restrictions must also be evaluated.

---

## Term Validation

Validate:

```text
termInMonths != null
termInMonths > 0
```

LoanType-specific term restrictions must also be evaluated.

---

## Destination Account Validation

When a destination account is required:

```text
destinationAccount != null
```

The account must:

```text
exist
be ACTIVE
allow credit operations
satisfy ownership rules
satisfy AccountType rules
satisfy Currency rules
```

---

## Initial Status

The service must establish:

```text
UNDER_REVIEW
```

through Domain behavior.

The caller must not be able to create an already-approved loan.

---

## Persistence

Persist through:

```text
LoanRepository
```

---

## Operation

Generate:

```text
OperationType.LOAN_APPLICATION
```

---

## Audit

Create the corresponding `AuditLog`.

The audit record must identify the relevant business operation and actor according to the Audit domain rules.

---

# 2. Consult Loan

## Description

Retrieves the authoritative Loan Domain Model.

---

## Input

```java
Loan consult(Loan loan);
```

The Domain Model must provide the required business identity/context.

Primitive identifiers must not be exposed as the application contract.

---

## Processing

```text
Loan
 │
 ▼
LoanRepository
 │
 ▼
Loan
```

---

## Authorization

The service must validate whether the requesting actor is allowed to access the Loan.

For customer access:

```text
User
  │
  ▼
Customer
  │
  ▼
Loan.applicant
```

The requesting Customer must have the required relationship with the Loan.

Employees may be authorized according to their `SystemRole`.

---

# 3. Evaluate Loan

## Description

Evaluates whether a Loan satisfies the Domain conditions necessary to proceed toward approval or rejection.

Evaluation should be conceptually separated from the actual state-changing approval or rejection operation.

---

## Input

```java
Loan evaluate(Loan loan);
```

or, when the evaluation is modeled as a decision:

```text
LoanEvaluationResult evaluate(Loan loan)
```

The latter is recommended if the evaluation must communicate a business decision without mutating the Loan.

---

## Status Validation

The Loan must be:

```text
UNDER_REVIEW
```

A Loan already:

```text
APPROVED
REJECTED
DISBURSED
CANCELLED
```

must not be evaluated as a new application.

---

## Customer Validation

Validate:

```text
Applicant exists
Applicant is ACTIVE
Applicant is eligible
```

---

## LoanType Validation

Validate:

```text
LoanType
```

and apply the policies associated with that type.

---

## Amount Validation

Validate:

```text
requestedAmount > 0
```

and all LoanType-specific amount constraints.

---

## Term Validation

Validate:

```text
termInMonths > 0
```

and all LoanType-specific term constraints.

---

## Interest Rate Validation

If an interest rate has already been established:

```text
interestRate >= 0
```

and the rate must satisfy LoanType-specific rules.

---

## Destination Account Validation

When applicable:

```text
AccountStatus == ACTIVE
```

and:

```text
Currency compatibility
AccountType compatibility
Ownership
```

must be evaluated.

---

## Evaluation Result

The Domain may produce:

```text
ApprovalDecision.APPROVED
ApprovalDecision.REJECTED
```

This is useful when evaluation itself does not mutate the Loan.

The recommended distinction is:

```text
Evaluate Loan
       │
       ▼
ApprovalDecision
       │
       ├── APPROVED ──► Approve Loan
       │
       └── REJECTED ──► Reject Loan
```

This avoids duplicating approval/rejection responsibilities.

---

# 4. Approve Loan

## Description

Approves a Loan that is currently under review.

The operation is restricted to:

```text
INTERNAL_ANALYST
```

---

## Input

```java
Loan approve(User user, Loan loan);
```

---

## User Validation

Validate:

```text
user != null
user.status == UserStatus.ACTIVE
user.role == SystemRole.INTERNAL_ANALYST
```

Authorization must occur inside the application/domain flow.

---

## Customer Validation

Validate:

```text
loan.applicant != null
loan.applicant.status == CustomerStatus.ACTIVE
```

The applicant must satisfy Loan eligibility rules.

---

## Loan Status Validation

The authoritative Loan must be:

```text
UNDER_REVIEW
```

Only:

```text
UNDER_REVIEW → APPROVED
```

is permitted.

---

## LoanType Validation

Validate:

```text
loan.loanType != null
```

and all LoanType-specific approval rules.

---

## Requested Amount Validation

Validate:

```text
requestedAmount != null
requestedAmount > 0
```

---

## Approved Amount Validation

Validate:

```text
approvedAmount != null
approvedAmount > 0
```

Unless explicitly permitted:

```text
approvedAmount <= requestedAmount
```

---

## Interest Rate Validation

Validate:

```text
interestRate != null
interestRate >= 0
```

and the applicable LoanType constraints.

---

## Term Validation

Validate:

```text
termInMonths != null
termInMonths > 0
```

---

## Destination Account Validation

When applicable:

```text
destinationAccount != null
destinationAccount.accountStatus == ACTIVE
```

and:

```text
AccountType
Currency
Ownership
```

must satisfy the applicable rules.

---

## Approval Date

The Domain establishes:

```text
approvalDate
```

at the moment of the valid transition:

```text
UNDER_REVIEW → APPROVED
```

The caller must not supply or override this date.

---

## Domain Behavior

The Loan Domain Model must execute a behavior equivalent to:

```text
loan.approve(...)
```

The Domain must verify the transition.

The application service must not simply execute:

```java
loan.setLoanStatus(LoanStatus.APPROVED);
```

without Domain transition rules.

---

## Persistence

Persist through:

```text
LoanRepository
```

---

## Operation

Generate:

```text
LOAN_APPROVAL
```

---

## Audit

Persist the corresponding audit record.

The actor must be the authorized `INTERNAL_ANALYST`.

---

# 5. Reject Loan

## Description

Rejects a Loan that is currently under review.

---

## Input

```java
Loan reject(User user, Loan loan);
```

---

## User Validation

Validate:

```text
User.status == ACTIVE
User.role == INTERNAL_ANALYST
```

---

## Loan Status Validation

The Loan must be:

```text
UNDER_REVIEW
```

The permitted transition is:

```text
UNDER_REVIEW → REJECTED
```

---

## Customer Validation

The applicant must exist.

Customer status must be validated when rejection rules require current Customer information.

---

## Domain Transition

The Domain must execute the rejection behavior.

The application service must not bypass the Domain transition mechanism.

---

## Persistence

Persist through:

```text
LoanRepository
```

---

## Operation

Generate:

```text
LOAN_REJECTION
```

---

## Audit

Generate the corresponding audit record.

---

# 6. Disburse Loan

## Description

Disburses the approved Loan amount to the destination `BankAccount`.

This is a state-changing financial operation and therefore requires strict validation and transactional consistency.

---

## Input

The current conceptual input is:

```java
Loan disburse(Loan loan);
```

However, if the disbursement must be attributed to a human User, the recommended contract is:

```java
Loan disburse(User user, Loan loan);
```

The final decision depends on whether disbursement is:

```text
User initiated
```

or:

```text
System initiated
```

If system initiated, the audit model must explicitly represent the system actor.

---

## Loan Status Validation

The authoritative Loan must be:

```text
APPROVED
```

The only permitted transition is:

```text
APPROVED → DISBURSED
```

---

## Approved Amount Validation

Validate:

```text
approvedAmount != null
approvedAmount > 0
```

---

## Destination Account Validation

Validate:

```text
destinationAccount != null
```

and retrieve authoritative account information when necessary.

---

## AccountStatus Validation

The account must be:

```text
ACTIVE
```

The following states reject disbursement:

```text
BLOCKED
CLOSED
```

---

## AccountType Validation

The destination account must satisfy the applicable LoanType restrictions.

---

## Currency Validation

The currency of the Loan and BankAccount must be compatible.

Unless FX is explicitly supported:

```text
Loan.currency == BankAccount.currency
```

---

## Ownership Validation

The Domain must validate the relationship between:

```text
Loan.applicant
```

and:

```text
BankAccount.owner
```

when the business requires the destination account to belong to the applicant.

---

## Duplicate Disbursement Validation

A Loan must not be disbursed more than once.

The service must validate:

```text
LoanStatus != DISBURSED
```

and the authoritative persisted state must be checked.

The operation must also be protected against concurrent duplicate execution.

---

## BankAccount Domain Behavior

The service must not modify:

```text
BankAccount.currentBalance
```

directly through infrastructure.

The BankAccount Domain behavior responsible for receiving funds must be used.

Conceptually:

```text
Loan
 │
 ▼
Disburse Loan
 │
 ├── Validate Loan
 │
 ├── Validate BankAccount
 │
 ├── Credit BankAccount
 │
 └── Transition Loan
```

---

## Cross-Aggregate Consideration

`Loan` and `BankAccount` are separate Domain Models.

If they are separate Aggregates, the Loan service must not bypass BankAccount's business rules by directly modifying the BankAccount aggregate.

A preferred conceptual approach is:

```text
Loan Service
      │
      ▼
Bank Account application/domain operation
      │
      ▼
BankAccount
```

or an appropriate Domain Output Port representing the business capability required to credit the account.

The exact approach depends on whether Loan and BankAccount belong to the same bounded context.

---

## Disbursement Date

The Domain establishes:

```text
disbursementDate
```

when the disbursement successfully occurs.

The caller must not provide this date.

---

## Transactional Consistency

The following changes must be consistent:

```text
Loan → DISBURSED
BankAccount → credited
Operation → LOAN_DISBURSEMENT
AuditLog → recorded
```

The system must prevent situations such as:

```text
Loan = DISBURSED
BankAccount = not credited
```

or:

```text
BankAccount = credited
Loan = APPROVED
```

The transaction strategy depends on the bounded-context architecture.

If all aggregates share one transactional boundary, a transaction may be used.

If they belong to different bounded contexts, an orchestration/event/outbox strategy may be required.

---

## Operation

Generate:

```text
LOAN_DISBURSEMENT
```

---

## Audit

Generate the corresponding audit event.

---

# 7. Cancel Loan

## Description

Cancels a Loan when its current state allows cancellation.

---

## Input

```java
Loan cancel(User user, Loan loan);
```

---

## Authorization

The authorization depends on:

```text
LoanStatus
UserStatus
SystemRole
```

The service must explicitly define which roles may cancel a Loan in each state.

The Domain must not rely solely on the controller for authorization.

---

## Valid Transitions

Current permitted transitions are:

```text
UNDER_REVIEW → CANCELLED
APPROVED → CANCELLED
OVERDUE → CANCELLED
```

Cancellation from:

```text
REJECTED
DISBURSED
CANCELLED
```

is not permitted under the current lifecycle.

---

## Outstanding Obligations

For cancellation after disbursement or during overdue status, the service must validate financial obligations.

The Loan must not be marked as cancelled merely by changing its status if cancellation requires:

* Outstanding balance settlement.
* Payment processing.
* Financial reversal.
* Contract termination.
* Penalties.
* Other business consequences.

Those rules must be explicitly defined by the Domain.

---

## Operation

Generate:

```text
LOAN_CANCELLATION
```

---

## Audit

Generate the corresponding audit record.

---

# 8. Consult Loan Status

## Description

Provides the authoritative current status of a Loan.

---

## Input

```java
Loan consultStatus(Loan loan);
```

or a richer Domain result containing the current Loan status.

---

## Processing

If the supplied Loan is authoritative:

```text
loan.loanStatus
```

may be returned directly.

If current persisted state is required:

```text
Loan
 │
 ▼
LoanRepository
 │
 ▼
Authoritative Loan
 │
 ▼
LoanStatus
```

The service must not trust stale state when current status is explicitly required.

---

## Authorization

The requesting actor must be authorized to consult the Loan.

---

# 9. Consult Loan Details

## Description

Returns the complete business representation of a Loan.

The result must remain a Domain representation.

Persistence entities must never cross the persistence adapter boundary.

---

## Input

```java
Loan consultDetails(Loan loan);
```

---

## Result

```text
Loan
```

The response must not expose:

```text
JPA Entity
Mongo Document
SQL Record
Persistence DTO
```

---

## Authorization

Access must be validated according to:

```text
Customer ownership
Employee role
User status
```

---

# 10. Validate Loan Eligibility

## Description

Determines whether the applicant and Loan application satisfy the business conditions required to proceed.

This operation should be cohesive and must not become a collection of unrelated technical validations.

---

## Input

```java
EligibilityResult validateEligibility(Loan loan);
```

A richer result is recommended when the system needs to communicate:

```text
Eligible
Not eligible
Reasons
Failed rules
```

The exact result model belongs to the Domain.

---

## Applicant Validation

Validate:

```text
Applicant exists
Applicant is ACTIVE
Applicant satisfies Customer eligibility rules
```

---

## LoanType Validation

Validate:

```text
LoanType exists
LoanType is supported
```

---

## Amount Validation

Validate:

```text
requestedAmount > 0
```

and applicable LoanType constraints.

---

## Term Validation

Validate:

```text
termInMonths > 0
```

and applicable LoanType constraints.

---

## Destination Account Validation

When applicable:

```text
Account exists
AccountStatus == ACTIVE
AccountType is permitted
Currency is compatible
Ownership is valid
```

---

# Evaluation vs Eligibility

The following distinction must be maintained:

```text
Validate Loan Eligibility
        │
        ▼
Determines whether business prerequisites are satisfied
```

while:

```text
Evaluate Loan
        │
        ▼
Performs the broader credit evaluation
        │
        ▼
ApprovalDecision
```

Eligibility does not necessarily imply approval.

For example:

```text
Customer eligible
+
Loan data valid
+
Account valid
```

does not necessarily mean:

```text
Loan APPROVED
```

The final approval decision remains a separate business action.

---

# Validation Matrix

| Service              | UserStatus                 | SystemRole              | CustomerStatus            | LoanType | LoanStatus                 | AccountStatus              | AccountType | Currency                 | BigDecimal               | Operation         |
| -------------------- | -------------------------- | ----------------------- | ------------------------- | -------- | -------------------------- | -------------------------- | ----------- | ------------------------ | ------------------------ | ----------------- |
| Request Loan         | Required when actor exists | Authorization-dependent | ACTIVE                    | Required | UNDER_REVIEW               | ACTIVE if account required | Applicable  | Required when applicable | Amount > 0               | LOAN_APPLICATION  |
| Consult Loan         | ACTIVE                     | Authorization-dependent | Access rule               | N/A      | Authoritative              | N/A                        | N/A         | N/A                      | N/A                      | None              |
| Evaluate Loan        | ACTIVE when actor exists   | Authorization-dependent | ACTIVE                    | Required | UNDER_REVIEW               | ACTIVE if required         | Applicable  | Compatible               | Amount > 0               | Usually none      |
| Approve Loan         | ACTIVE                     | INTERNAL_ANALYST        | ACTIVE                    | Required | UNDER_REVIEW               | ACTIVE if required         | Applicable  | Compatible               | Amount > 0; approved > 0 | LOAN_APPROVAL     |
| Reject Loan          | ACTIVE                     | INTERNAL_ANALYST        | Applicable                | Required | UNDER_REVIEW               | N/A                        | N/A         | N/A                      | Valid application        | LOAN_REJECTION    |
| Disburse Loan        | ACTIVE if user initiated   | Authorized role         | ACTIVE/valid relationship | Required | APPROVED                   | ACTIVE                     | Applicable  | Match                    | approvedAmount > 0       | LOAN_DISBURSEMENT |
| Cancel Loan          | ACTIVE                     | State-dependent         | Applicable                | Required | Allowed cancellation state | Applicable                 | Applicable  | Applicable               | Financial obligations    | LOAN_CANCELLATION |
| Consult Status       | ACTIVE                     | Authorization-dependent | Access rule               | N/A      | Authoritative              | N/A                        | N/A         | N/A                      | N/A                      | None              |
| Consult Details      | ACTIVE                     | Authorization-dependent | Access rule               | N/A      | Authoritative              | N/A                        | N/A         | N/A                      | N/A                      | None              |
| Validate Eligibility | ACTIVE when actor exists   | Authorization-dependent | ACTIVE                    | Required | Applicable                 | ACTIVE if required         | Applicable  | Compatible               | Amount > 0               | None              |

---

# Value Object Validation Matrix

| Value Object       | Loan Usage                          | Validation                                      |
| ------------------ | ----------------------------------- | ----------------------------------------------- |
| `SystemRole`       | User authorization                  | Must be a valid controlled role                 |
| `CustomerStatus`   | Applicant eligibility               | Normally `ACTIVE` for new processing            |
| `UserStatus`       | Actor authorization                 | Normally `ACTIVE`                               |
| `LoanStatus`       | Loan lifecycle                      | Must follow valid transitions                   |
| `AccountStatus`    | Destination account                 | Must be `ACTIVE` for disbursement               |
| `AccountType`      | Destination account rules           | Must be a supported account type                |
| `LoanType`         | Loan classification                 | Must be a supported LoanType                    |
| `Currency`         | Loan/account monetary compatibility | Must be valid and compatible                    |
| `OperationType`    | Operation registration              | Must correspond to the executed business action |
| `ApprovalDecision` | Evaluation result                   | `APPROVED` or `REJECTED`                        |

---

# OperationType Mapping

Each state-changing Loan service must generate the appropriate `OperationType`.

```text
Request Loan
    ↓
LOAN_APPLICATION

Approve Loan
    ↓
LOAN_APPROVAL

Reject Loan
    ↓
LOAN_REJECTION

Disburse Loan
    ↓
LOAN_DISBURSEMENT

Cancel Loan
    ↓
LOAN_CANCELLATION
```

Future Loan services may generate:

```text
LOAN_PAYMENT
LOAN_OVERDUE
```

These operation types already exist in the Value Object catalog.

Therefore, if those operations are in scope, corresponding Loan business services must be defined.

---

# Loan Payment Gap

The current `OperationType` catalog includes:

```text
LOAN_PAYMENT
```

but the current Loan Services catalog does not define a:

```text
Pay Loan
```

service.

This is a domain completeness gap.

If Loan payments are within the scope of the system, the Loan subdomain should eventually define a service such as:

```text
Pay Loan
```

with the corresponding:

```text
PayLoanUseCase
```

and a Domain Model representing the payment operation.

Money must continue to be represented with:

```text
BigDecimal
```

---

# Loan Overdue Gap

The `LoanStatus` catalog includes:

```text
OVERDUE
```

and the `OperationType` catalog includes:

```text
LOAN_OVERDUE
```

Therefore the system must define how a Loan enters:

```text
DISBURSED → OVERDUE
```

This should not necessarily be an arbitrary manual User operation.

An overdue transition normally depends on:

```text
Payment due date
Outstanding obligation
Payment state
Current date
```

If the system requires automatic overdue detection, the architecture should define an appropriate application process or scheduled mechanism.

The Domain remains responsible for determining whether the transition is valid.

---

# Loan Cancellation vs Loan Closure

The current Loan lifecycle uses:

```text
CANCELLED
```

but the original service list contained:

```text
Close Loan
```

These concepts must not be treated as synonyms.

Cancellation means:

```text
The loan contract/process is terminated according to cancellation rules.
```

Closure normally means:

```text
The loan has been fully settled or completed.
```

If the business requires a true closure state, the Domain Value Object catalog must explicitly include:

```text
CLOSED
```

and define its lifecycle.

Until that is explicitly added, the official Loan service is:

```text
Cancel Loan
```

with:

```text
CancelLoanUseCase
```

---

# Output Ports

The Loan subdomain may require:

```text
LoanRepositoryPort
CustomerRepositoryPort
BankAccountRepositoryPort
OperationRepositoryPort
AuditLogRepositoryPort
```

## Canonical port naming

The names `LoanRepository`, `CustomerRepository`, `BankAccountRepository`,
`OperationRepository`, and `AuditRepository` used historically in this
document are aliases of the canonical ports defined in
`backendbackendSDD/Domain/Output-ports.md`:

| Name used in this document | Canonical Output Port |
|---|---|
| `LoanRepository` | `LoanRepositoryPort` |
| `CustomerRepository` | `CustomerRepositoryPort` |
| `BankAccountRepository` | `BankAccountRepositoryPort` |
| `OperationRepository` | `OperationRepositoryPort` |
| `AuditRepository` | `AuditLogRepositoryPort` |

New implementations must use the canonical names.

## Service-to-Port Matrix

| Service | LoanRepositoryPort | CustomerRepositoryPort | BankAccountRepositoryPort | OperationRepositoryPort | AuditLogRepositoryPort |
|---|---:|---:|---:|---:|---:|
| Request Loan | ✓ (save) | ✓ | ✓ (destination account) | ✓ | ✓ |
| Consult Loan | ✓ (read) | | | | |
| Evaluate Loan | ✓ (read when required) | ✓ | ✓ (when applicable) | | |
| Approve Loan | ✓ (update) | ✓ | ✓ (destination account) | ✓ | ✓ |
| Reject Loan | ✓ (update) | ✓ (when required) | | ✓ | ✓ |
| Disburse Loan | ✓ (update) | | ✓ (credit account) | ✓ | ✓ |
| Cancel Loan | ✓ (update) | | | ✓ | ✓ |
| Consult Loan Status | ✓ (read) | | | | |
| Consult Loan Details | ✓ (read) | | | | |
| Validate Loan Eligibility | ✓ (read when required) | ✓ | ✓ (when applicable) | | |

An empty cell means the service does not require that Output Port. Authorization is composed through the Authorization subdomain services (e.g. `AuthorizeLoanOperationService`, `AuthorizeLoanApprovalService`).

These interfaces belong to the application/domain boundary according to the Hexagonal Architecture adopted by the system.

---

# LoanRepository

## Description

Provides persistence capabilities for the Loan Domain Model.

Conceptually:

```java
interface LoanRepository {

    Loan save(Loan loan);

    Loan find(Loan loan);

    boolean exists(Loan loan);
}
```

The exact contract must be refined according to the identity and retrieval requirements of each use case.

The repository must return:

```text
Loan
```

and not:

```text
LoanEntity
```

---

# CustomerRepository

## Description

Provides authoritative Customer information when the supplied `Customer` Domain Model does not contain everything required by a business rule.

Conceptually:

```java
interface CustomerRepository {

    Customer find(Customer customer);

    boolean exists(Customer customer);
}
```

The repository must not expose persistence entities.

---

# BankAccountRepository

## Description

Provides authoritative BankAccount information when Loan operations require current account state.

It is particularly relevant for:

```text
Destination account validation
Loan disbursement
Account status
Account type
Currency
Ownership
```

Conceptually:

```text
Loan Service
     │
     ▼
BankAccountRepositoryPort
     │
     ▼
BankAccount Adapter
     │
     ▼
Persistence
```

---

# OperationRepository

## Description

Persists business operations generated by the Loan lifecycle.

Examples:

```text
LOAN_APPLICATION
LOAN_APPROVAL
LOAN_REJECTION
LOAN_DISBURSEMENT
LOAN_CANCELLATION
LOAN_PAYMENT
LOAN_OVERDUE
```

The repository must receive the `Operation` Domain Model.

---

# AuditRepository

## Description

Persists audit information generated by significant Loan business actions.

The Loan service must not access MongoDB directly.

Conceptually:

```text
Loan Service
     │
     ▼
AuditRepositoryPort
     │
     ▼
Mongo Adapter
```

Audit severity should follow the centralized Audit domain policy.

---

# Input Ports

The Loan subdomain exposes:

```text
RequestLoanUseCase
ConsultLoanUseCase
EvaluateLoanUseCase
ApproveLoanUseCase
RejectLoanUseCase
DisburseLoanUseCase
CancelLoanUseCase
ConsultLoanStatusUseCase
ConsultLoanDetailsUseCase
ValidateLoanEligibilityUseCase
```

If payments are added:

```text
PayLoanUseCase
```

If automatic overdue processing is added:

```text
MarkLoanOverdueUseCase
```

may be introduced according to the final architecture.

---

# Example Input Ports

```java
interface RequestLoanUseCase {

    Loan request(Loan loan);
}
```

```java
interface ConsultLoanUseCase {

    Loan consult(Loan loan);
}
```

```java
interface EvaluateLoanUseCase {

    LoanEvaluationResult evaluate(Loan loan);
}
```

```java
interface ApproveLoanUseCase {

    Loan approve(User user, Loan loan);
}
```

```java
interface RejectLoanUseCase {

    Loan reject(User user, Loan loan);
}
```

```java
interface DisburseLoanUseCase {

    Loan disburse(User user, Loan loan);
}
```

If the business confirms that disbursement is system-driven, the contract may instead remain:

```java
Loan disburse(Loan loan);
```

but the audit model must explicitly represent the system actor.

```java
interface CancelLoanUseCase {

    Loan cancel(User user, Loan loan);
}
```

```java
interface ConsultLoanStatusUseCase {

    LoanStatus consultStatus(Loan loan);
}
```

```java
interface ConsultLoanDetailsUseCase {

    Loan consultDetails(Loan loan);
}
```

```java
interface ValidateLoanEligibilityUseCase {

    LoanEligibilityResult validateEligibility(Loan loan);
}
```

---

# Domain Behavior

The Loan Domain Model should expose business behavior instead of unrestricted setters.

Conceptually:

```text
Loan
 │
 ├── approve()
 ├── reject()
 ├── disburse()
 ├── cancel()
 └── markOverdue()
```

The Domain methods are responsible for enforcing valid state transitions.

For example:

```text
loan.approve()
```

must internally ensure:

```text
loanStatus == UNDER_REVIEW
```

before transitioning to:

```text
APPROVED
```

Likewise:

```text
loan.disburse()
```

must only be valid when:

```text
loanStatus == APPROVED
```

---

# REST Boundary

The REST layer may receive DTOs.

However:

```text
REST Controller
      │
      ▼
Request DTO
      │
      ▼
Mapper
      │
      ▼
Loan Domain Model
      │
      ▼
Input Port
```

DTOs must never enter the Domain.

The Controller must not implement:

```text
Customer eligibility
LoanStatus transitions
AccountStatus validation
Authorization rules
Currency rules
LoanType business rules
```

These responsibilities belong to the appropriate Domain/Application components.

---

# Persistence Boundary

The persistence adapter translates between:

```text
Loan Domain Model
```

and:

```text
Persistence Entity
```

Conceptually:

```text
Loan Domain
     │
     ▼
LoanRepositoryPort
     │
     ▼
LoanPersistenceAdapter
     │
     ▼
LoanEntity
     │
     ▼
Database
```

The Domain must remain unaware of:

```text
JPA
Hibernate
MySQL
MongoDB
SQL
Spring Data
```

---

# Loan Application Flow

```text
HTTP Request
     │
     ▼
Request DTO
     │
     ▼
Request Mapper
     │
     ▼
Loan Domain Model
     │
     ▼
RequestLoanUseCase
     │
     ▼
Loan Application Service
     │
     ├── Validate Customer
     ├── Validate LoanType
     ├── Validate Amount
     ├── Validate Term
     ├── Validate Account
     ├── Validate Currency
     ├── Apply Domain Behavior
     │
     ├── LoanRepository
     ├── OperationRepository
     └── AuditRepository
```

---

# Loan Approval Flow

```text
User + Loan
     │
     ▼
ApproveLoanUseCase
     │
     ▼
Loan Application Service
     │
     ├── Validate UserStatus
     ├── Validate SystemRole
     ├── Validate CustomerStatus
     ├── Validate LoanType
     ├── Validate Amount
     ├── Validate Term
     ├── Validate InterestRate
     ├── Validate DestinationAccount
     ├── Validate LoanStatus
     │
     ▼
Loan.approve()
     │
     ├── LoanRepository
     ├── OperationRepository
     └── AuditRepository
```

---

# Loan Rejection Flow

```text
User + Loan
     │
     ▼
RejectLoanUseCase
     │
     ▼
Loan Application Service
     │
     ├── Validate UserStatus
     ├── Validate SystemRole
     ├── Validate LoanStatus
     │
     ▼
Loan.reject()
     │
     ├── LoanRepository
     ├── OperationRepository
     └── AuditRepository
```

---

# Loan Disbursement Flow

```text
User/System + Loan
       │
       ▼
DisburseLoanUseCase
       │
       ▼
Loan Application Service
       │
       ├── Validate User/System Actor
       ├── Validate LoanStatus
       ├── Validate ApprovedAmount
       ├── Validate BankAccount
       ├── Validate AccountStatus
       ├── Validate AccountType
       ├── Validate Currency
       ├── Validate Ownership
       ├── Validate Duplicate Disbursement
       │
       ▼
BankAccount Domain Behavior
       │
       ▼
Loan.disburse()
       │
       ├── LoanRepository
       ├── BankAccount operation
       ├── OperationRepository
       └── AuditRepository
```

---

# Operation and Audit Pattern

Every significant Loan lifecycle operation follows:

```text
Business Action
      │
      ▼
Domain State Change
      │
      ├──────────────► Loan
      │
      ▼
Operation
      │
      ▼
AuditLog
```

For example:

```text
Approve Loan
     │
     ├── Loan → APPROVED
     │
     ├── Operation → LOAN_APPROVAL
     │
     └── AuditLog
```

The Loan service must not directly manipulate the persistence representation of either:

```text
Operation
AuditLog
```

---

# Operation Consistency

An Operation must represent an actual business action.

The service must not create:

```text
LOAN_APPROVAL
```

if the Loan was not successfully approved.

Likewise:

```text
LOAN_DISBURSEMENT
```

must not be created if the funds were not successfully disbursed.

Operation creation must therefore occur as part of the same consistent business transaction/process.

---

# Audit Requirements

Audit information should allow the system to determine at minimum:

```text
What operation occurred?
Which Loan was affected?
Who or what performed it?
When did it occur?
What was the relevant business state?
```

The exact structure belongs to the Audit domain.

The Loan service is responsible for invoking the appropriate audit Output Port.

---

# Exception Catalog

Conceptual Loan exceptions include:

```text
LoanNotFoundException
InvalidLoanException
InvalidLoanTypeException
InvalidLoanStatusException
InvalidLoanStatusTransitionException

LoanAlreadyApprovedException
LoanAlreadyRejectedException
LoanAlreadyDisbursedException
LoanAlreadyCancelledException

CustomerNotEligibleException
CustomerInactiveException
CustomerBlockedException

UserNotAuthorizedException
UserInactiveException
UserBlockedException

InvalidLoanAmountException
InvalidApprovedAmountException
InvalidInterestRateException
InvalidLoanTermException

InvalidDestinationAccountException
DestinationAccountNotActiveException
DestinationAccountOwnershipException
InvalidDestinationAccountTypeException

CurrencyMismatchException

LoanDisbursementException
OutstandingLoanObligationException
```

Technical exceptions such as:

```text
SQLException
JPAException
MongoException
HttpClientException
```

must not become part of the Domain API.

They belong to the infrastructure/application error-handling strategy.

---

# Architectural Constraints

The following rules are mandatory for the Loan subdomain.

1. `Loan` is a Domain Model.

2. `Loan` inherits from `BankingProduct`.

3. `Loan.applicant` is represented by `Customer`.

4. `Loan.destinationAccount` is represented by `BankAccount`.

5. Customer relationships must not be represented only by primitive identifiers.

6. Bank Account relationships must not be represented only by primitive identifiers.

7. Loan services must receive Domain Models or Value Objects.

8. Loan services must not receive primitive identifiers as application-level parameters.

9. Loan services must not receive isolated attributes when those attributes belong to a Domain Model.

10. REST DTOs must never enter the Domain layer.

11. Persistence entities must never enter the Domain layer.

12. Business validations must be implemented in the appropriate Domain/Application components.

13. Value Objects must enforce their own structural/business invariants.

14. Controlled catalog values must not be replaced with arbitrary strings.

15. `LoanType` must be treated as a controlled Domain Value Object.

16. `LoanStatus` must be treated as a controlled Domain Value Object.

17. `Currency` must be treated as a controlled Domain Value Object.

18. `AccountType` must be treated as a controlled Domain Value Object.

19. `AccountStatus` must be treated as a controlled Domain Value Object.

20. `CustomerStatus` must be treated as a controlled Domain Value Object.

21. `UserStatus` must be treated as a controlled Domain Value Object.

22. `SystemRole` must be treated as a controlled Domain Value Object.

23. `OperationType` must be treated as a controlled Domain Value Object.

24. `ApprovalDecision` is a primitive enum and must not be confused with `LoanStatus`.

25. Monetary values must remain `BigDecimal`.

26. `double` and `float` must not be used for monetary values.

27. Requested amounts must be greater than zero.

28. Approved amounts must be greater than zero when a Loan is approved.

29. Approved amounts must not exceed requested amounts unless explicitly permitted by business rules.

30. Interest rates must satisfy their Domain constraints.

31. Loan terms must be positive.

32. Currency compatibility must be validated before disbursement.

33. A destination account must be active before disbursement.

34. A blocked account must not receive Loan disbursement.

35. A closed account must not receive Loan disbursement.

36. Destination account ownership must be validated when required by business rules.

37. LoanType-specific rules must be implemented through Domain policies/specifications when required.

38. User status must be validated before protected User operations.

39. Customer status must be validated before customer eligibility operations.

40. Authorization must validate both User status and role.

41. `INTERNAL_ANALYST` is required for Loan approval.

42. `INTERNAL_ANALYST` is required for Loan rejection.

43. Loan status transitions must be enforced by the Domain.

44. `UNDER_REVIEW` is the initial Loan lifecycle state.

45. A rejected Loan must not be disbursed.

46. A Loan must not be disbursed before approval.

47. A Loan must not be disbursed more than once.

48. A Loan must not be cancelled from an invalid state.

49. Approval date must be established by the Domain transition.

50. Disbursement date must be established by the Domain transition.

51. Callers must not arbitrarily control lifecycle dates.

52. Callers must not arbitrarily control lifecycle status.

53. Authoritative persisted state must be retrieved when required for state-changing operations.

54. Loan services must use Output Ports for external information.

55. Loan services must never access MySQL directly.

56. Loan services must never access MongoDB directly.

57. Loan services must never execute SQL directly.

58. Loan services must never access JPA directly.

59. Persistence adapters must remain responsible for persistence translation.

60. `LoanRepository` is responsible for Loan persistence.

61. `CustomerRepository` provides Customer information when required.

62. `BankAccountRepository` provides BankAccount information when required.

63. `OperationRepository` persists business operations.

64. `AuditRepository` persists audit information.

65. Significant Loan lifecycle events must generate an `Operation`.

66. Relevant Loan operations must be recorded in the `AuditLog`.

67. An Operation must correspond to an actual successful business action.

68. Loan state changes and their corresponding operations/audits must maintain transactional consistency.

69. Loan must not directly manipulate database balances.

70. BankAccount business behavior must be respected when crediting a destination account.

71. Cross-Aggregate interactions must respect Aggregate boundaries.

72. The Domain must remain independent of infrastructure technology.

73. The Loan subdomain must be testable without MySQL.

74. The Loan subdomain must be testable without MongoDB.

75. The Loan subdomain must be testable without REST.

76. The Loan subdomain must be testable without JPA.

77. The persistence layer must never determine business status transitions.

78. Controllers must not contain Loan business rules.

79. DTO mappers must not contain Loan business rules.

80. Repository adapters must not contain Loan business rules.

---

# Final Loan Service Pattern

The canonical pattern for Loan state-changing services is:

```text
                  ┌──────────────────────┐
                  │    Domain Models     │
                  │ User / Loan /        │
                  │ Customer / Account   │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │    Input Port        │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │   Application        │
                  │      Service         │
                  └──────────┬───────────┘
                             │
             ┌───────────────┼────────────────┐
             │               │                │
             ▼               ▼                ▼
       Validate User   Validate Customer   Validate Loan
             │               │                │
             └───────────────┼────────────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Validate Value       │
                  │ Objects & Policies   │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Validate State       │
                  │ Transition           │
                  └──────────┬───────────┘
                             │
                             ▼
                  ┌──────────────────────┐
                  │ Domain Behavior      │
                  │ approve / reject /   │
                  │ disburse / cancel    │
                  └──────────┬───────────┘
                             │
             ┌───────────────┼────────────────┐
             │               │                │
             ▼               ▼                ▼
       LoanRepository  OperationRepository AuditRepository
             │               │                │
             └───────────────┼────────────────┘
                             ▼
                      Consistent Result
```

The essential architectural rule is:

```text
Domain Model
     ↓
Input Port
     ↓
Application Service
     ↓
Domain Validation + Domain Behavior
     ↓
Output Ports
     ↓
Adapters
```

and never:

```text
Controller
     ↓
Database
```

or:

```text
Service
     ↓
JPA / SQL / MongoDB
```

The Loan subdomain therefore remains aligned with:

```text
Hexagonal Architecture
        +
Domain-Driven Design
        +
Domain-first design
        +
Dependency Inversion
        +
High Cohesion
        +
Low Coupling
        +
Technology Independence
```
